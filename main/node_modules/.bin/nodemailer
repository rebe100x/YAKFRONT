#!/bin/sh
if [ -x "`dirname "$0"`/node" ]; then
  "`dirname "$0"`/node"  "`dirname "$0"`/../nodemailer/bin/nodemailer" "$@"
  ret=$?
else 
  node  "`dirname "$0"`/../nodemailer/bin/nodemailer" "$@"
  ret=$?
fi
exit $ret
